## Change Detection on Satellite Images using CUDA

#### A project to detect changes in satellite images using CUDA
```
// run this to build and execute
cd /build cmake -S ../src/ -B . && make && ./bin/main
```
